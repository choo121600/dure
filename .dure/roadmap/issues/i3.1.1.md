---
id: i3.1.1
slug: survey-scaffold
type: issue
title: dure-survey.py scaffold — JSON/exit contract + survey config defaults + robust loaders
status: done
github: null
milestone: m3
epic: e3.1
labels: [epic:e3.1]
acceptance:
  - Given any repo, when dure-survey.py runs, then it emits JSON {status, counts, findings:[{check,severity,id,message}]} consistent with dure-audit/dure-status, with exit 0 (advisory) / 1 (finding severity >= survey.fail_on) / 2 (internal error)
  - Given a config.yml with no survey section, then the default fail_on=error is applied (hard-coded)
  - Given an empty/new project with no .dure/roadmap/ directory, then zero findings and exit 0
  - Given an item with malformed front matter, then it is silently skipped (status treated as unknown, not done) and NO integrity/schema finding is emitted (that is dure-doctor's class)
---

# I3.1.1 — dure-survey.py scaffold

CLI + JSON output contract + exit codes, consistent with `dure-doctor.py`/`dure-audit.py`/`dure-status.py`.
Item loaders MUST reuse the audit/status union-membership convention so the survey can never disagree
with them. The `survey:` config default (`fail_on: error`) MUST be hard-coded so the tool works when the
section is absent. No signals yet — this issue fixes the contract and the robustness edges.
