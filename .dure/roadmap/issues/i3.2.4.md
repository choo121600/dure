---
id: i3.2.4
slug: direction-skill
type: issue
title: /dure:direction skill — orchestration
status: todo
github: null
milestone: m3
epic: e3.2
labels: [epic:e3.2]
acceptance:
  - A /dure:direction skill (skills/direction/SKILL.md) runs dure-survey/dure-audit/dure-status as evidence, runs the deep-interview loop to converge a direction, writes a .dure/directions/<slug>.md doc, validates it with dure-proposal.py, and hands the chosen direction to /dure:plan
  - The skill is report-only at the validator boundary (the direction doc is a non-canonical sketch; /dure:plan re-derives the canonical roadmap; the skill itself MUST NOT write .dure/roadmap/)
  - The skill frontmatter passes test_plugin_skills.py (strict YAML name + description, referenced scripts exist)
---

# I3.2.4 — /dure:direction skill

The orchestration surface built on the validator: gather survey/audit/status evidence → run the deep
interview targeting *which direction* → write `.dure/directions/<slug>.md` → validate via
`dure-proposal.py` (gate-PASS embedded) → hand the chosen direction to `/dure:plan`, which re-derives the
canonical issues. Built last in E3.2, on top of i3.2.1–3.
