---
id: i3.1.4
slug: survey-disjointness-skill-ci
type: issue
title: Disjointness test (AC-c) + /dure:survey skill + CI wiring
status: todo
github: null
milestone: m3
epic: e3.1
labels: [epic:e3.1]
acceptance:
  - A test asserts dure-survey's check-class set {closable-milestone, empty-epic} shares no class name with dure-doctor's violation classes, dure-audit's 4 implemented checks (todo-marker, untested-script, done-parent-undone-child, unfinished-interview), or dure-status's reported fields (AC-c)
  - A /dure:survey skill (skills/survey/SKILL.md) runs dure-survey.py and surfaces findings as planning evidence (report-only; never writes .dure/roadmap/); skill frontmatter passes test_plugin_skills.py
  - CI runs tests/test_dure_survey.py (existing tests/test_*.py glob) and a dure-survey self-check on the committed repo asserting exit 0 with zero findings
---

# I3.1.4 — disjointness guard + skill + CI

The ownership-invariant guard (AC-c) plus the user-facing surface: the `/dure:survey` slash command and
CI integration. The disjointness test enumerates audit's 4 IMPLEMENTED check classes (not the stale
3-check spec list) and doctor's + status's classes, asserting `dure-survey` owns a disjoint set.
