---
id: i3.2.1
slug: proposal-scaffold-structure
type: issue
title: dure-proposal.py scaffold + directions schema + structural presence checks
status: todo
github: null
milestone: m3
epic: e3.2
labels: [epic:e3.2]
acceptance:
  - Given a .dure/directions/<slug>.md doc, when dure-proposal.py runs, then it emits JSON {status, violations:[{check, message}]} with exit 0 (no violations) / 1 (>=1 violation) / 2 (internal error)
  - Given a complete gate-PASS direction doc, then status is pass with zero violations and exit 0
  - Given a doc missing or emptying each section in turn (no problem, fewer than 2 options, an option with no critique, no chosen marker, empty rationale, no candidate issue, an empty acceptance string), then exactly the matching direction class fires (direction:problem-missing, direction:options-too-few, direction:option-critique-missing, direction:chosen-missing, direction:rationale-missing, direction:candidate-issue-missing, direction:acceptance-missing)
  - No check is named or asserts testable or valid — every check is presence/non-emptiness only
  - Given any run, then the entire .dure/ tree is byte-for-byte unchanged before and after (validator writes nothing anywhere under .dure/)
---

# I3.2.1 — dure-proposal.py scaffold + structural checks

CLI + JSON/exit contract consistent with dure-audit/dure-survey, plus the `.dure/directions/<slug>.md`
schema and the structural presence checks (problem, >=2 options each with a critique, chosen direction
with rationale, >=1 candidate-issue sketch with a non-empty acceptance string). Candidate issues are a
NON-canonical sketch (no roadmap id/slug/status). Report-only: MUST NOT write under `.dure/`. The gate
check lands in i3.2.2.
