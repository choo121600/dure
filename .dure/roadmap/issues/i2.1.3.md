---
id: i2.1.3
slug: check-untested-script
type: issue
title: Check — untested-script (warning)
status: todo
github: null
milestone: m2
epic: e2.1
labels: [epic:e2.1]
acceptance:
  - Given scripts/dure-*.py, when a matching tests/test_<base>.py is absent (base mapped hyphen->underscore), then a warning finding is reported
  - Given audit.untested_allowlist, when a script is listed, then it is skipped (no finding)
  - Given the committed repo, then untested-script yields zero findings (dure-gate allowlisted; all others have tests)
---

# I2.1.3 — untested-script check

`scripts/dure-*.py` lacking `tests/test_<base>.py` (base name mapped `-`→`_`), minus
`audit.untested_allowlist` (default `[dure-gate]`). Warning severity.
