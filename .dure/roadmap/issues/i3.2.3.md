---
id: i3.2.3
slug: proposal-disjointness-ci
type: issue
title: Disjointness test (direction:* five-way) + CI wiring
status: done
github: null
milestone: m3
epic: e3.2
labels: [epic:e3.2]
acceptance:
  - A test enumerates dure-proposal.py's direction class set from a fixture tripping each check, asserts it is non-empty, and asserts it shares no class name with dure-doctor, dure-audit (4 classes), dure-survey (2 classes), or dure-status fields (extends the four-way invariant to five)
  - The disjointness harness reads violations[].check via the exact keys dure-proposal.py emits (guards against a vacuous pass on an empty set)
  - CI runs tests/test_dure_proposal.py (existing tests/test_*.py glob) and the repo self-checks remain green
---

# I3.2.3 — five-way disjointness + CI

Extend the ownership invariant to five tools: assert `dure-proposal.py`'s `direction:*` class set is
disjoint from doctor / audit / survey / status. Mirror the survey AC-c pattern (enumerate the full set,
guard non-emptiness, then assert `isdisjoint`). Wire the new test suite into CI.
