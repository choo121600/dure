---
id: i2.1.5
slug: audit-tests
type: issue
title: Tests — AC-a/b/c incl. dure-doctor ownership disjointness
status: todo
github: null
milestone: m2
epic: e2.1
labels: [epic:e2.1]
acceptance:
  - AC-a — fixtures {clean, one-real-TODO, dure-*.py without test, done-milestone-with-todo-issue} each yield the exact expected findings + JSON shape; exit 0 by default and 1 when fail_on is lowered
  - AC-b — on the committed repo, zero false positives (todo-marker=0, untested-script respects allowlist + hyphen mapping, no dure-doctor-class overlap), under python3
  - AC-c — dure-audit check-classes are disjoint from dure-doctor check-classes (asserted by test)
---

# I2.1.5 — tests

`tests/test_dure_audit.py` covering AC-a/b/c, including a disjointness assertion against
`dure-doctor`'s check-class set.
