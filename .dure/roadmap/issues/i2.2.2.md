---
id: i2.2.2
slug: audit-suggest
type: issue
title: dure-audit --suggest — draft issue stubs from findings (report-only)
status: todo
github: null
milestone: m2
epic: e2.2
labels: [epic:e2.2]
acceptance:
  - Given findings, when dure-audit.py --suggest runs, then the output includes a suggestions[] of {check,severity,title,rationale,acceptance[]} for each warning-or-higher finding
  - info-severity findings (todo-marker) are NOT turned into suggestions by default
  - --suggest is report-only — it MUST NOT create issues or write to .dure/roadmap/
  - on the committed repo, --suggest yields zero suggestions (no warning-or-higher findings)
---

# I2.2.2 — dure-audit --suggest

Turn warning-or-higher findings into draft issue stubs (title + rationale + suggested acceptance)
the user can feed into `/dure:plan`. Report-only and deterministic; the user decides what becomes work.
