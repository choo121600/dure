---
id: i2.1.1
slug: audit-scaffold
type: issue
title: dure-audit.py scaffold — JSON/exit contract + hard-coded audit config defaults
status: todo
github: null
milestone: m2
epic: e2.1
labels: [epic:e2.1]
acceptance:
  - Given any repo, when dure-audit.py runs, then it emits JSON {status, findings:[{check,severity,file?,line?,id?,message}], counts}
  - Given audit.fail_on, when a finding severity >= fail_on then exit is 1, else 0; internal error is 2
  - Given a config.yml with no audit section, then defaults (untested_allowlist=[dure-gate], fail_on=error) are applied
---

# I2.1.1 — dure-audit.py scaffold

CLI + JSON output contract + exit codes, consistent with `dure-doctor.py`/`dure-status.py`.
The `audit:` config defaults MUST be hard-coded so the tool works when the section is absent.
